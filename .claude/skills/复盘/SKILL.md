---
name: 复盘
description: >
  通用发布后数据复盘+闭环。给定创作者档案(profiles/{id})对发布的视频按其北极星判定。
  当用户说"复盘""看这条数据""这条表现怎么样""为什么扑了""更新套路库"时触发。
  取数→按 profile.north_star 判定→留存归因→限流预警→出复盘卡→给套路库更新建议(人工确认)。
---

# 发布后复盘（闭环后半截 · 通用）

> 判据全读 `profiles/{id}/profile.json` 的 `north_star`/`signal_thresholds`,引擎在 `engine/`。

## 流程
1. **取数+判定一条命令**(抖音,自动读浏览器 cookie,需浏览器登录抖音):
   ```bash
   .venv/bin/python engine/fetch/pull.py review {id} --latest      # 最新一条
   .venv/bin/python engine/fetch/pull.py review {id} --item-id X   # 指定视频
   ```
   直接出完整复盘卡(含留存/播放来源)。⚠️ 主战非抖音的号:自动取数暂不支持,让用户贴后台数据,手动喂 `engine/review.py {id}`(吃 raw json)。
   - 指标表/命中分母/"不看XX" 全按 `profile.north_star`+`anti_metrics`(迪迪看涨粉率+完播;职场琳看加微率+完播)。
   - 判定逻辑见 `engine/judge.py`(floor 合格线 + target 达标 + 小样本护栏<500)。
3. **限流优先排查**:复盘卡标 🚨疑似限流(0播放有留存/无推荐流)→ **先查可见性/标签/红线**(对红线库),别归因内容。
4. **归因**:留存曲线分离——开头(前5s)掉得比同类狠=钩子/选题;中段掉=结构/节奏。2秒跳出>0.4=钩子弱。
5. **回填**:这条用了哪些套路、评分卡当初几分、预测卡 vs 实际 diff。
6. **更新建议**:套路库(`profiles/{id}/套路库.md`)加权/降权、预测器校准、评分卡维度调整。
7. **👤 人工卡点**:建议先给用户,**确认后才改套路库**。

## 闭环（回到选题）
复盘结论喂回:验证好的套路 → `profiles/{id}/套路库.md` 加权 → 下轮「选题」优先;扑的方向降权/避开。
**这是流水线的回路**:选题→文案→视频→复盘→**回选题**,转一圈比上一圈准。

## 铁律
- **单条不下结论**:同类攒 2-3 条再改库(判数据看时间线,<48h 不判爆/扑)。
- 数据 24-72h 才稳;建议 24h/72h/7天 各取一次。
- 判据来自 profile,**不写死任何指标**——换创作者复盘逻辑自动跟着 north_star 变。

## 参考
- 引擎 `engine/review.py` `judge.py` ｜ 北极星枚举+判定 `profiles/_schema/北极星枚举.md`
- 限流排查对 `共享机制/红线库/`
